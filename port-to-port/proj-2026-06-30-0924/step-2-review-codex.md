Clean. `git diff --name-only` shows only `port-to-port/run_baseten_sweep.sh` plus `port-to-port/proj-2026-06-30-0924/PLAN.md`, with no staged diff; the script diff is limited to the two-line 8192 comment and `MAX_TOKENS="${MAX_TOKENS:-4096}"` becoming `MAX_TOKENS="${MAX_TOKENS:-8192}"`, while the PLAN edits are progress/status-only. The new default flows as intended: `run_baseten_sweep.sh` passes `--max-tokens "$MAX_TOKENS"` to `mini-rl-env.py`, `mini-rl-env.py` parses `--max-tokens` as an optional int, permits it for `--provider openai`, carries `args.max_tokens` into `LLMServiceConfig(max_tokens=...)`, and `llm_factory.py` writes it through as `params_kwargs["max_tokens"] = int(max_tokens)` after removing `max_completion_tokens`; I found no Baseten-path hard clamp below 8192. Non-Baseten defaults are unaffected because `mini-rl-env.py` still defaults `--max-tokens` to `None`, other sweep scripts retain their own explicit token settings, and this `MAX_TOKENS` shell default is local to `run_baseten_sweep.sh`.
